load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

sh_binary(
    name = "dev",
    srcs = ["dev.sh"],
    args = ["$(location @nodejs//:npm)"],
    data = ["@nodejs//:npm"],
    deps = ["//dev/lib:util-bash"],
)
