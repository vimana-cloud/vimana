load("@rules_rust//rust:defs.bzl", "rust_test")

rust_test(
    name = "success-test",
    srcs = ["success-test.rs"],
    deps = [
        "//work/runtime:metadata-prost",
        "//work/runtime:names",
        "//work/runtime/decode",
        "@crates//:bytes",
        "@crates//:tonic",
        "@crates//:wasmtime",
    ],
)
