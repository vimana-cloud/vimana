---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: servers.api.vimana.host
spec:
  group: api.vimana.host
  names:
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the servers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerSpec defines the desired state of Server
            properties:
              auth:
                description: Authentication configuration for the server.
                properties:
                  jwks:
                    description: URLs of JSON web key set that can be used to validate
                      JWTs on incoming requests.
                    items:
                      type: string
                    type: array
                type: object
              features:
                additionalProperties:
                  type: object
                description: Map from feature flag names to configurations.
                type: object
              id:
                description: User-provided unique ID of the server.
                type: string
              reflection:
                description: |-
                  Whether gRPC reflection is enabled for all the services on this server.
                  If this is enabled,
                  then neither `grpc.reflection.v1.ServerReflection` nor `grpc.reflection.v1alpha1.ServerReflection`
                  may be specified under `Services`.
                type: boolean
              services:
                description: List of fully-qualified gRPC service names served by
                  this server.
                items:
                  type: string
                type: array
              versionWeights:
                additionalProperties:
                  format: int32
                  type: integer
                description: |-
                  Map from version strings to traffic weights.
                  The traffic proportion is the weight divided by the total of all weights.
                type: object
            required:
            - auth
            - id
            type: object
          status:
            description: ServerStatus defines the observed state of Server
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
