---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: domains.api.vimana.host
spec:
  group: api.vimana.host
  names:
    kind: Domain
    listKind: DomainList
    plural: domains
    singular: domain
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Domain is the Schema for the domains API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DomainSpec defines the desired state of a Domain.
            properties:
              aliases:
                description: List of alias domain names.
                items:
                  type: string
                type: array
              failover:
                description: List of domain names to forward traffic to in case of
                  an outage.
                items:
                  type: string
                type: array
              grpc:
                description: gRPC-specific configuration for the domain.
                properties:
                  reflection:
                    description: |-
                      Enabling gRPC reflection
                      serves the special `grpc.reflection.v1.ServerReflection` service
                      which provides a spec of all the services reachable on that domain.
                    properties:
                      allBut:
                        description: |-
                          Enable reflection for every service except these full-named services.
                          Incompatible with `noneBut`.
                        items:
                          type: string
                        type: array
                      noneBut:
                        description: |-
                          Only enable reflection for these full-named services.
                          Incompatible with `allBut`.
                          The default is empty (reflection never enabled).
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              id:
                description: Unique ID of the domain, as a hex-encoded string.
                type: string
              openApi:
                description: |-
                  Provide an auto-generated OpenAPI Description at `/.well-known/schema.json`
                  covering all the HTTP-transcoded methods of all the servers in the domain.
                type: boolean
              regions:
                description: |-
                  Subset of regions in which servers within this domain may run.
                  If empty, they could run anywhere globally.
                  Array of names.
                items:
                  type: string
                type: array
            required:
            - id
            type: object
          status:
            description: DomainStatus defines the observed state of a Domain.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
