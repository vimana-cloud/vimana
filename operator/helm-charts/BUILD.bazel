load("//operator:build-tools.bzl", "pkg_directory")

pkg_directory(
    name = "helm-charts",
    subdirectories = [
        "component",
        "domain",
        "server",
        "vimana",
    ],
)
