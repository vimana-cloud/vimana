apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vimana.fullname" . }}-test-connection"
  labels:
    {{- include "vimana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vimana.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
