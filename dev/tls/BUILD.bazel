sh_binary(
    name = "generate",
    srcs = ["generate.sh"],
)

# Used for end-to-end testing.
# To regenerate: `bazel run //dev/tls:generate`.
exports_files(
    [
        "api.vimana.host.cert",
        "api.vimana.host.key",
    ],
    visibility = ["//visibility:public"],
)
