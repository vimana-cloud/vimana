// The most basic end-to-end test
// verifies that a tiny component with a single unary RPC can run.

syntax = "proto3";

package foo;

service ThisOldTrope {
  rpc HelloWorld(HelloRequest) returns (HelloResponse) {}
}

message HelloRequest {
  string name = 1;
}

message HelloResponse {
  string message = 1;
}
