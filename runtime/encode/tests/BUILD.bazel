load("@rules_rust//rust:defs.bzl", "rust_test")

rust_test(
    name = "success-test",
    srcs = ["success-test.rs"],
    deps = [
        "//runtime:metadata-prost",
        "//runtime:names",
        "//runtime/encode",
        "@crates//:bytes",
        "@crates//:tonic",
        "@crates//:wasmtime",
    ],
)
